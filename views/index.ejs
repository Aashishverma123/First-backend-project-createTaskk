<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

  <div class="main w-full h-screen bg-zinc-800">
    <div class="form p-3 text-white">
      <form action="/create" method="post">
        <input class="w-full px-5 py-2 rounded-md outline-none bg-zinc-700" type="text" placeholder="File name" name="title">
        <textarea class="w-full px-5 py-1 rounded-md outline-none bg-zinc-700 resize-none mt-3" placeholder="Write details in file" name="details"></textarea>
        <input class="px-10 py-2 tracking-tighter mt-5 bg-teal-900 rounded-md" type="submit" placeholder="submit">
      </form>
    </div>
    
    <div class="TaskContainer p-3 flex gap-10 flex-wrap">
      <% if(files.length> 0) { %>
        <% files.forEach(function(file) { %>
          
          <div class=" task w-[13vw] bg-zinc-900 py-2 px-3 rounded-md text-white">
            <h1 class="text-xl tracking-tighter">
              <%= file.name %>
               </h1>
            <p class="mt-2 text-xs">
              <% if(file.content.length> 15) { %>
                <%= file.content.substring(0, 16) + '...' %>
                    <% } else { %>
                        <%= file.content %>
                            <% } %>
              </p>
              <div class="mt-5 flex justify-between">
                <a class="text-[.5vw] bg-blue-500 py-1 px-1 rounded-md inline-block tracking-tighter font-bold " href="/read/<%= file.name %> ">Read more....</a>

                <div class="flex gap-3">
                  <a class="text-xs  inline-block tracking-tighter font-bold  " href="/edit/<%= file.name %>">Edit</a>
                  <a class="text-xs  inline-block tracking-tighter font-bold  " href="/delete/<%= file.name %> ">Delete</a>

                </div>

              </div>
          </div>    

       <% }) %>
       <%} else{ %>
        
        <h1 class="text-zinc-600 mt-10 p-3">No tasks yet</h1>
         
       <% } %>

  </div>
  
</body>
</html>
